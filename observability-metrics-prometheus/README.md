## Usage

Install this module in your OpenChoreo Kubernetes cluster by using Helm

```bash
helm install observability-metrics-prometheus ./helm --namespace openchoreo-observability-plane --create-namespace
```

